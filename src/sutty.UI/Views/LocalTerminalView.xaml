<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="sutty.UI.Views.LocalTerminalView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:sutty.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource TerminalBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="14,7"
            BorderBrush="{ThemeResource Divider}"
            BorderThickness="0,1,0,1">
            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    FontSize="15"
                    Foreground="{ThemeResource AccentTeal}"
                    Glyph="&#xE756;" />

                <Grid Grid.Column="1" HorizontalAlignment="Left"
                      VerticalAlignment="Center" ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center" Spacing="1">
                        <TextBlock
                            x:Name="TitleText"
                            FontFamily="Cascadia Mono, Consolas"
                            FontSize="11.5"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource TextPrimary}"
                            TextTrimming="CharacterEllipsis"
                            Text="PowerShell" />
                        <TextBlock
                            x:Name="SubtitleText"
                            FontSize="9.5"
                            TextTrimming="CharacterEllipsis"
                            Foreground="{ThemeResource TextMuted}" />
                    </StackPanel>
                    <Border
                        x:Name="StatusPill"
                        Grid.Column="1"
                        Padding="8,2"
                        VerticalAlignment="Center"
                        CornerRadius="10">
                        <TextBlock
                            x:Name="StatusPillText"
                            FontSize="10.5"
                            FontWeight="SemiBold" />
                    </Border>
                </Grid>

                <StackPanel
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="6">
                    <TextBlock
                        x:Name="TerminalStatusText"
                        MaxWidth="160"
                        VerticalAlignment="Center"
                        FontFamily="Cascadia Mono, Consolas"
                        FontSize="10.5"
                        Foreground="{ThemeResource TextMuted}"
                        TextTrimming="CharacterEllipsis"
                        Text="CONPTY 120&#x00D7;40" />
                    <Button
                        x:Name="CopyOutputButton"
                        Width="30"
                        Height="30"
                        MinWidth="0"
                        MinHeight="0"
                        Padding="6"
                        Background="{ThemeResource AccentGradient}"
                        BorderThickness="0"
                        CornerRadius="8"
                        Foreground="{ThemeResource AccentForeground}"
                        Click="CopyOutput_Click">
                        <Button.Resources>
                            <StaticResource x:Key="ButtonBackgroundPointerOver" ResourceKey="AccentGradientHover" />
                            <StaticResource x:Key="ButtonBackgroundPressed" ResourceKey="AccentGradient" />
                            <StaticResource x:Key="ButtonForegroundPointerOver" ResourceKey="AccentForeground" />
                            <StaticResource x:Key="ButtonForegroundPressed" ResourceKey="AccentForeground" />
                        </Button.Resources>
                        <FontIcon FontSize="15" Foreground="{ThemeResource AccentForeground}" Glyph="&#xE8C8;" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <controls:TerminalRendererControl
            x:Name="TerminalSurface"
            Grid.Row="1"
            AutomationProperties.Name="Local PowerShell terminal"
            Loaded="TerminalSurface_Loaded" />
    </Grid>
</UserControl>
