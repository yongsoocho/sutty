# Alpha 4 SSH route evidence / Alpha 4 SSH 경로 증거

Accepted bundles: **0**. No route or forwarding row is **Live Validated** or **Released** here.

인수 완료 bundle: **0개**. 여기에서 **Live Validated** 또는 **Released**로 승격한 route·forwarding 행은 없습니다.

This scope accepts reviewed bundles for `ROUTE-LIVE-001` (HTTP CONNECT/SOCKS), `ROUTE-LIVE-002` (SSH jump/ProxyCommand), and `TUN-LIVE-001` (local/remote/dynamic forwarding). Every route, authentication, and forwarding-mode combination remains a separate result. See the [evidence schema](../../EVIDENCE_SCHEMA.md).

이 범위에는 `ROUTE-LIVE-001`(HTTP CONNECT/SOCKS), `ROUTE-LIVE-002`(SSH jump/ProxyCommand), `TUN-LIVE-001`(local/remote/dynamic forwarding)의 검토 완료 bundle만 둡니다. Route·인증·forwarding mode 조합마다 별도 결과를 유지합니다. [증거 스키마](../../EVIDENCE_SCHEMA.md)를 따릅니다.
