# Alpha 4 SSH authentication evidence / Alpha 4 SSH 인증 증거

Accepted bundles: **0**. No authentication method or key-format row is **Live Validated** or **Released** here.

인수 완료 bundle: **0개**. 여기에서 **Live Validated** 또는 **Released**로 승격한 인증 방식·키 형식 행은 없습니다.

This scope accepts reviewed bundles for `SSH-LIVE-001` (Direct Password), `SSH-LIVE-002` (separate public-key format/encryption combinations), `SSH-LIVE-003` (Agent), and `SSH-LIVE-004` (keyboard-interactive). Never combine different authentication methods or material key variants into one `Pass`. See the [evidence schema](../../EVIDENCE_SCHEMA.md).

이 범위에는 `SSH-LIVE-001`(Direct Password), `SSH-LIVE-002`(분리된 공개키 형식·암호화 조합), `SSH-LIVE-003`(Agent), `SSH-LIVE-004`(keyboard-interactive)의 검토 완료 bundle만 둡니다. 서로 다른 인증 방식이나 중요한 키 변형을 하나의 `Pass`로 합치지 않습니다. [증거 스키마](../../EVIDENCE_SCHEMA.md)를 따릅니다.
