# Alpha 4 SSH connection-information evidence / Alpha 4 SSH 연결 정보 증거

Accepted bundles: **0**. No negotiated-information row is **Live Validated** or **Released** here.

인수 완료 bundle: **0개**. 여기에서 **Live Validated** 또는 **Released**로 승격한 협상 정보 행은 없습니다.

This scope accepts reviewed `SSH-INFO-001` bundles for the displayed handshake fields, independently checked host identity, reconnect freshness, disconnect/fault clearing, indirect-route behavior, and absence of automatic exec requests. Actual fingerprints, endpoints, transcripts, and command output are forbidden. See the [evidence schema](../../EVIDENCE_SCHEMA.md).

이 범위에는 표시한 handshake 필드, 독립적으로 확인한 Host identity, reconnect freshness, disconnect/fault 뒤 정보 제거, 간접 route, 자동 exec 요청 부재를 검증한 `SSH-INFO-001` bundle만 둡니다. 실제 지문, endpoint, transcript, command output은 금지합니다. [증거 스키마](../../EVIDENCE_SCHEMA.md)를 따릅니다.
